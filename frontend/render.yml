
Set-Content render.yaml "services:
  - type: web
    name: breathe-esg-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://breathe-esg-3udp.onrender.com/"